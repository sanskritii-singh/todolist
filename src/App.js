// App.js
import React from "react";
import TodoApp from "./todoapp"; // make sure the path matches your file structure
import "./todoapp.css"; // this ensures the styles are applied

function App() {
  return <TodoApp />;
}

export default App;
